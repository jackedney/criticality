/**
 * Test fixture: Class with methods containing TODO markers.
 */

export class Calculator {
  add(a: number, b: number): number {
    throw new Error('TODO');
  }

  subtract(a: number, b: number): number {
    return a - b;
  }
}
