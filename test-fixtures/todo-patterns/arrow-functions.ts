/**
 * Test fixture: Arrow functions with TODO markers.
 */

export const increment = (n: number): number => {
  throw new Error('TODO');
};

export const decrement = (n: number): number => {
  return n - 1;
};
